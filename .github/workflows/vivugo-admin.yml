name: ViVuGo Admin CI

on:
  push:
    paths:
      - "vivugo-admin/**"
      - ".github/workflows/vivugo-admin.yml"
  pull_request:
    paths:
      - "vivugo-admin/**"
      - ".github/workflows/vivugo-admin.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: vivugo-admin
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: vivugo-admin/package-lock.json
      - run: npm ci
      - run: npm run build
